spring:
  datasource:
    url: jdbc:h2:mem:opsmcp;MODE=PostgreSQL;INIT=RUNSCRIPT FROM 'classpath:schema-test.sql'
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_uuid

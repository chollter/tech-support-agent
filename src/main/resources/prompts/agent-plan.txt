请基于抽取结果和用户原文，规划 Incident 调查步骤。

输出 JSON（不要 markdown 代码块）：
{
  "actions": ["KNOWLEDGE_SEARCH", "QUERY_METRIC", "QUERY_LOGS"],
  "skipped": ["SIMILAR_CASE_SEARCH"],
  "reason": "OOM 场景优先查 metric 和 logs"
}

规则：
- actions / skipped 只能使用以下枚举：KNOWLEDGE_SEARCH、SIMILAR_CASE_SEARCH、QUERY_LOGS、QUERY_METRIC
- actions 是有序执行步骤；skipped 是明确跳过的步骤
- OOM/内存类优先 QUERY_METRIC + QUERY_LOGS，可跳过 SIMILAR_CASE_SEARCH
- 连接池/超时类优先 QUERY_LOGS
- 至少包含 KNOWLEDGE_SEARCH，除非信息已极度充分（极少情况）
- 不要编造不存在的工具或步骤
